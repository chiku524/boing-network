---
import Layout from '../layouts/Layout.astro';

const apps = [
  {
    name: 'boing.finance',
    description: 'The flagship DeFi platform for the Boing ecosystem. Swap, bridge, deploy tokens, and manage liquidity across EVM and Solana.',
    category: 'DeFi',
    url: 'https://boing.finance',
    status: 'live',
    featured: true,
  },
  {
    name: 'Boing Network',
    description: 'The foundation layer — run a node, stake, participate in governance. The authentic L1 blockchain powering the ecosystem.',
    category: 'Infrastructure',
    url: '/',
    status: 'live',
    featured: true,
  },
  // Placeholder for future apps
  {
    name: 'Your dApp',
    description: 'Build on Boing. Native account abstraction, adaptive gas, and success-based incentives for developers.',
    category: 'Ecosystem',
    url: '/developers/quickstart',
    status: 'build',
    featured: false,
  },
];
---

<Layout title="Ecosystem" description="The DeFi that always bounces back. Explore applications and dApps on Boing Network — boing.finance, infrastructure, and the growing ecosystem." canonical="/applications">
  <article class="applications-page">
    <header class="page-header">
      <h1 class="gradient-text-hero">Ecosystem</h1>
      <p class="lead">The DeFi that always bounces back. Boing Network is the foundation. Applications built on it form the ecosystem — DeFi, infra, and beyond.</p>
    </header>

    <section class="apps-grid">
      {apps.map((app) => (
        <a
          href={app.url}
          class={`app-card ${app.featured ? 'featured' : ''}`}
          target={app.url.startsWith('http') ? '_blank' : undefined}
          rel={app.url.startsWith('http') ? 'noopener' : undefined}
        >
          <div class="app-header">
            <h3>{app.name}</h3>
            <span class={`category ${app.category.toLowerCase()}`}>{app.category}</span>
          </div>
          <p class="app-desc">{app.description}</p>
          <div class="app-footer">
            <span class={`status status-${app.status}`}>{app.status}</span>
            <span class="arrow">→</span>
          </div>
        </a>
      ))}
    </section>

    <section class="build-cta">
      <div class="cta-card">
        <h2>Build on Boing</h2>
        <p>Native account abstraction, adaptive gas, success-based dApp incentives. Deploy tokens, create liquidity, and earn through usage.</p>
        <div class="cta-links">
          <a href="/developers/quickstart" class="btn btn-primary">Developer Quickstart</a>
          <a href="/docs/getting-started" class="btn btn-secondary">Run a Node</a>
        </div>
      </div>
    </section>

    <section class="ecosystem-note">
      <p>More applications will appear here as the ecosystem grows. Interested in listing your dApp? <a href="/community">Join the community</a>.</p>
    </section>
  </article>
</Layout>

<style>
  .applications-page {
    max-width: 900px;
    margin: 0 auto;
    padding: 2rem;
  }

  .page-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .page-header h1 {
    font-size: clamp(2rem, 4vw, 2.5rem);
    margin: 0 0 0.5rem;
    filter: drop-shadow(0 0 20px var(--glow-cyan-subtle));
  }

  .lead {
    font-size: 1.125rem;
    color: var(--text-secondary);
  }

  .apps-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  .app-card {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: 1rem;
    padding: 1.5rem;
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    transition: all 0.2s;
  }

  .app-card:hover {
    border-color: var(--accent-neon);
    box-shadow: 0 0 24px var(--glow-cyan-subtle);
  }

  .app-card.featured {
    border-color: var(--primary-color);
    box-shadow: 0 0 20px rgba(59, 130, 246, 0.2);
  }

  .app-card.featured:hover {
    border-color: var(--accent-neon);
  }

  .app-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 0.75rem;
  }

  .app-header h3 {
    font-size: 1.125rem;
    margin: 0;
    color: var(--text-primary);
  }

  .category {
    font-size: 0.7rem;
    text-transform: uppercase;
    font-weight: 600;
    padding: 0.2em 0.5em;
    border-radius: 4px;
  }

  .category.defi {
    background: rgba(139, 92, 246, 0.2);
    color: #a78bfa;
  }

  .category.infrastructure {
    background: rgba(6, 182, 212, 0.2);
    color: var(--accent-neon);
  }

  .category.ecosystem {
    background: rgba(59, 130, 246, 0.2);
    color: #60a5fa;
  }

  .app-desc {
    font-size: 0.9rem;
    color: var(--text-secondary);
    margin: 0 0 1rem;
    flex: 1;
    line-height: 1.5;
  }

  .app-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 0.85rem;
  }

  .status {
    text-transform: capitalize;
  }

  .status-live {
    color: #34d399;
  }

  .status-build {
    color: var(--accent-neon);
  }

  .app-footer .arrow {
    color: var(--accent-neon);
  }

  .build-cta {
    margin-bottom: 2rem;
  }

  .cta-card {
    background: linear-gradient(135deg, var(--bg-card) 0%, var(--bg-tertiary) 100%);
    border: 1px solid var(--border-color);
    border-radius: 1rem;
    padding: 2rem;
    text-align: center;
  }

  .cta-card h2 {
    font-size: 1.5rem;
    margin: 0 0 0.75rem;
    color: var(--text-primary);
  }

  .cta-card p {
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
  }

  .cta-links {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s;
  }

  .btn-primary {
    background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
    color: white;
  }

  .btn-secondary {
    background: transparent;
    color: var(--accent-neon);
    border: 1px solid var(--accent-neon);
  }

  .btn-secondary:hover {
    background: rgba(0, 224, 255, 0.1);
  }

  .ecosystem-note {
    text-align: center;
    font-size: 0.9rem;
    color: var(--text-tertiary);
  }
</style>
