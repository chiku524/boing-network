# Used by: wrangler pages deploy dist (Pages does not support --config)
# Static site only — no Functions, so no bindings here. Avoids "Failed to publish your Function".
# For Workers/API with D1, R2, KV use: wrangler deploy --config wrangler.worker.toml
name = "boing-network"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"
